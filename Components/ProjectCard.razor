<div class="project-card @(Featured ? "featured" : "")">
    @if (Featured)
    {
        <span class="featured-badge">Projet phare</span>
    }
    @if (!string.IsNullOrEmpty(VideoUrl))
    {
        <video class="project-thumbnail" src="@VideoUrl" poster="@ImageUrl"
               autoplay muted loop playsinline preload="metadata"></video>
    }
    else
    {
        <img src="@ImageUrl" alt="@Title" class="project-thumbnail" />
    }
    <div class="project-info">
        <h3>@Title</h3>
        <p>@Description</p>
        <div class="project-tags">
            @foreach (var tag in Tags)
            {
                <span class="pill">@tag</span>
            }
        </div>
        @if (IsPrivate)
        {
            <span class="private-badge"><i class="bi bi-lock-fill" aria-hidden="true"></i> Privé</span>
        }
        else
        {
            <a href="@Link" target="@(IsExternalLink ? "_blank" : null)" rel="@(IsExternalLink ? "noopener noreferrer" : null)" class="btn-styled @(Featured ? "btn-featured" : "")">
                @LinkLabel
                @if (Featured)
                {
                    <i class="bi bi-arrow-right" aria-hidden="true"></i>
                }
            </a>
        }
    </div>
</div>

@code {
    [Parameter] public string Title { get; set; } = "";
    [Parameter] public string Description { get; set; } = "";
    [Parameter] public string ImageUrl { get; set; } = "";
    [Parameter] public string Link { get; set; } = "#";
    [Parameter] public string LinkLabel { get; set; } = "Voir";
    [Parameter] public List<string> Tags { get; set; } = new();
    [Parameter] public bool IsPrivate { get; set; } = false;
    [Parameter] public string VideoUrl { get; set; } = "";
    [Parameter] public bool Featured { get; set; } = false;

    private bool IsExternalLink => Link.StartsWith("http", StringComparison.OrdinalIgnoreCase);
}
